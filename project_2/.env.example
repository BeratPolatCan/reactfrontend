# Backend'in public URL'i (sonunda / OLMADAN).
# Boş bırakılırsa kod "http://localhost:8080" kullanır (local geliştirme).
# Vercel'de bu değişkeni deploy edilmiş backend adresinle doldur:
#   VITE_API_BASE_URL=https://senin-backend.up.railway.app
VITE_API_BASE_URL=
